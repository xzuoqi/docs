import loader_data from './loader.json'

:::tip
SPI/U-boot/loader 文件和 SoC 型号相关, 可根据产品对应的 SoC 型号去下面列表中去找。
:::

<p>{props.series ? loader_data[props.series].text : null}</p>
<ul>
{
 props.series ? loader_data[props.series].images.map((item,key)=>{
    return <li key={key}>
      <a href={item.url}>{item.title}</a>
    </li>
  }) : null
}
</ul>
